{
    "version": 2,
    "functions": {
        "api/server.ts": {
            "includeFiles": "prisma/**,swagger.yaml,public/**"
        }
    },
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/api/server"
        }
    ]
}