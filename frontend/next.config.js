/** @type {import('next').NextConfig} */
const nextConfig = {
  // Rewrites are handled by the root vercel.json to avoid conflicts between Next.js and the Express API
}

module.exports = nextConfig

module.exports = nextConfig
